# Smart Traffic Analyzer v2 — Python dependencies
# Install with:  pip install -r requirements.txt

# ── Core ML & vision ──────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
# YOLOv11 is part of ultralytics >= 8.3.0
# Model weights (yolo11n.pt, yolo11s.pt etc.) are downloaded automatically on first run.
ultralytics>=8.3.0
opencv-python>=4.8.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# ── Object tracking ───────────────────────────────
deep-sort-realtime>=1.3.2

# ── RL training ───────────────────────────────────
stable-baselines3>=2.2.0
gymnasium>=0.29.0
tensorboard>=2.14.0

# ── Prediction ────────────────────────────────────
# All transformer/GNN code is pure PyTorch; no extra deps needed

# ── Configuration ─────────────────────────────────
pyyaml>=6.0.1

# ── Explainability (optional) ─────────────────────
# shap>=0.44.0   # uncomment for full SHAP support

# ── Utilities ─────────────────────────────────────
tqdm>=4.66.0
matplotlib>=3.8.0
Pillow>=10.1.0
albumentations>=1.3.1